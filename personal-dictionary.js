function search(){
    // Go through all the elements of arr to look for item.
    for (var i = 0; i < arr.length; i++) {
      if (arr[i] === item) { // Found it!
        return i;
      }
    }
    
    // Item not found in the array.
    return null;
  }
    


function addWord(){

}

int[] words = [word1, word2, word3, word4];

export let search
export let addWord